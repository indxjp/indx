# Architecture

The search platform has two core components: the search-pipeline that ingests and
ranks documents, and the vector-store that persists embeddings. Read both pages to
understand the full request path.
