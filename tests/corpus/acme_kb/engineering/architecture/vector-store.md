# Vector Store

The vector store persists chunk embeddings and serves nearest-neighbor queries.
It supports cosine similarity over normalized vectors and an approximate index for
low-latency retrieval at scale.
