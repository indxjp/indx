# Testing Strategy

The testing strategy layers fast unit tests, corpus tests against real documents,
and end-to-end tests against live services. Unit tests run on every commit; the
heavier tiers run nightly and on release tags.
